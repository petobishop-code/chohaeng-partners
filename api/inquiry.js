export default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({message:"Method not allowed"});const d=req.body||{};if(!d.name||!d.phone||!d.consent)return res.status(400).json({message:"필수값을 확인해주세요."});const clean={receivedAt:new Date().toISOString(),name:String(d.name).slice(0,80),phone:String(d.phone).slice(0,40),budget:d.budget||"",experience:d.experience||"",interests:d.interests||"",message:String(d.message||"").slice(0,2000),consent:true};const endpoint=process.env.DB_WEBHOOK_URL;if(!endpoint)return res.status(503).json({message:"DB_WEBHOOK_URL is not configured"});try{const r=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(clean)});if(!r.ok)throw new Error("Webhook failed");return res.status(200).json({ok:true})}catch(e){return res.status(502).json({message:"DB transfer failed"})}}